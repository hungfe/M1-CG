script
alert
string= number= string
string -,*,/ number=number
Toan tu so Hoc: =,-,*,/,%
Toan tu so sanh: <,>,<=,>=,==
Toan tu gan: =,+=,-=,*=,/=,%=
Toan tu logic: &&,&,|,||,!
<input type="text" id="s1">
<input type="text" id="s2">
<button onclick="doanCode1()">Tinh cho Tao</button>
<h1 id="kq"></h1>
<h1 id="kq2"></h1>
<script>
  function doanCode1() {
    let c = +document.getElementById('s1').value;
    let d = +document.getElementById('s2').value;
    let T = (c+d);
    let S = (c*d);
    let C = (T*2);
    document.getElementById('kq').innerHTML = "Dien Tich la: " + c * d;
    document.getElementById('kq2').innerHTML = "Chu vi la: " + T * 2;
  }
  // aleet(a); Hiê thị hộp thông báo
  // document.write(a); Hiển thị lên web
  // console.log(a); Hiển thị trong màn hình console
  // document.get.ElementById('Ket qua'); Hiển thij lên thẻ có id Ket qua
  // const; Khai báo hằng số
  // let isLick; Boolean là kểu dưc liệu có 2 giá tri; true false
  // a++; Tăng một đơn vị<=> a=a+1; Hậu tố ưu tiên gán trước tăng sau
  // ++a; Tiền tố Ưu tiên tăng trước gán sau
  // window,innerWedth: Chieu ngang man hinh
  // khi ảnh chạm cạnh phải màn hình thì ngay lap tuc, quay laại cạnh trái rồi đi tiếp



